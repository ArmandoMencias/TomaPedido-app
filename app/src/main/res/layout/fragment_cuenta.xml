<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    android:background="#F0F2F5"> <!-- Fondo gris claro consistente -->

    <!-- Cabecera con Icono de Cuenta -->
    <LinearLayout
        android:id="@+id/headerCuenta"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <ImageView
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:src="@android:drawable/ic_menu_agenda"
            app:tint="#333333" />

        <TextView
            android:id="@+id/tvTituloCuenta"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:text="Detalle de Cuenta"
            android:textSize="24sp"
            android:textStyle="bold"
            android:textColor="#333333" />
    </LinearLayout>

    <!-- Contenedor tipo "Ticket" para la lista -->
    <androidx.cardview.widget.CardView
        android:id="@+id/cardLista"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginTop="20dp"
        android:layout_marginBottom="16dp"
        app:cardCornerRadius="16dp"
        app:cardElevation="4dp"
        app:layout_constraintTop_toBottomOf="@id/headerCuenta"
        app:layout_constraintBottom_toTopOf="@id/layoutTotal">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rvCuentaDetalle"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:padding="8dp"
            tools:listitem="@android:layout/simple_list_item_2" />
    </androidx.cardview.widget.CardView>

    <!-- Sección del Total -->
    <LinearLayout
        android:id="@+id/layoutTotal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:paddingVertical="12dp"
        android:gravity="end|center_vertical"
        app:layout_constraintBottom_toTopOf="@id/btnAgregarMas">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="TOTAL A PAGAR:"
            android:textSize="14sp"
            android:textColor="#757575"
            android:textStyle="bold"
            android:layout_marginEnd="12dp"/>

        <TextView
            android:id="@+id/tvTotalCuenta"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="$0.00"
            android:textSize="32sp"
            android:textStyle="bold"
            android:textColor="#2E7D32" /> <!-- Verde oscuro para el dinero -->
    </LinearLayout>

    <!-- Botón Agregar más (Estilo Mesero - Azul) -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnAgregarMas"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:layout_marginBottom="12dp"
        android:text="AGREGAR MÁS PRODUCTOS"
        android:textSize="16sp"
        android:textStyle="bold"
        app:cornerRadius="12dp"
        app:icon="@android:drawable/ic_input_add"
        app:iconGravity="textStart"
        android:backgroundTint="#1565C0"
        app:layout_constraintBottom_toTopOf="@id/btnCerrarCuenta" />

    <!-- Botón Cerrar (Estilo Alerta/Cobro - Rojo/Naranja) -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnCerrarCuenta"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:text="CERRAR CUENTA Y COBRAR"
        android:textSize="16sp"
        android:textStyle="bold"
        app:cornerRadius="12dp"
        app:icon="@android:drawable/ic_menu_send"
        app:iconGravity="textStart"
        android:backgroundTint="#D32F2F"
        app:layout_constraintBottom_toBottomOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>